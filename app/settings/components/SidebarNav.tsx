interface SidebarNavProps {
    items: {
        title: string,
        href: string,
    }[]
}

export default function SidebarNav({ items }: SidebarNavProps) {

    return (
        <div>test</div>
    )
}